"""Stage: ``companion_nature_likelihood``.

Continuous joint likelihood over the true nature of M2 from broadband photometry, Gaia XP
residuals, and SB2 characteristics; feeds ``population_model`` as a per-system weight, never as a
pre-filter (ARCHITECTURE.md §4). Not yet implemented.
"""
