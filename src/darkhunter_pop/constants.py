"""True physical constants for darkhunter_pop.

Only quantities that are not choosable live here: ``astropy.constants`` re-exports where
available, plus named extras (e.g. Chandrasekhar mass). Thresholds, method switches, priors, and
calibration scatter belong in ``config.yaml`` — see ARCHITECTURE.md §2 and §7.

Not yet implemented beyond the package layout stub.
"""
