"""Entry point: prints the run plan, then executes stages in order via ``run_management``.

Subagent #16 in the roster; blocked until the Foundation layer's interfaces are frozen
(ORCHESTRATION_PLAN.md §4). Not yet implemented.
"""
