# Environment management: venv (ORCHESTRATION_PLAN.md §2).
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -e ".[dev,plot,inference]"
#
# gaiamock is vendored as a pinned git submodule under vendor/gaiamock, not installed from PyPI.
# dark-hunter_rv and dark-hunter_sed are installed editable from their sibling checkouts.

numpy>=1.26,<2.0
scipy>=1.11
astropy>=6.0
astroquery>=0.4
h5py>=3.10
pydantic>=2.6
pyyaml>=6.0
dynesty>=2.1
matplotlib>=3.8
pytest>=8.0
